Drop MinecraftTen font binary here.

Preferred:  MinecraftTen-VF.woff2  (variable axis, ~80 KB)
Fallback:   MinecraftTen.woff2
Fallback:   MinecraftTen.ttf

The @font-face rule in app/globals.css will pick up whichever is present.
If no file is provided, the site falls back to Space Grotesk cleanly.
